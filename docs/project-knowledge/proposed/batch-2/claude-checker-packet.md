# Independent Claude Checker Packet — Batch 2

Status: CHECKER REQUEST — REVIEW THE EXACT COMMIT, NOT THIS MAKER SUMMARY

Candidate commit: use the immutable Draft PR #5 head SHA recorded in the PR description. Refuse a branch name or moving target.

Claude must independently inspect the Batch 2 source package, Batch 1 accepted source/normalization baseline, the machine-readable Batch 2 records, and this normalization review.

## Required checks

1. Verify all four source/metadata files remain source-only and that original DOCX fidelity is explicitly unresolved.
2. Confirm exactly ten machine records exist, every record and package is `PROPOSED` and `governing: false`, and no source self-label or cross-batch comparison can promote anything.
3. Verify the component delta specializes rather than replaces Batch 1's 24-component taxonomy.
4. Verify all eight conflicts, twelve missing-material items, and nine dependency groups are represented consistently.
5. Confirm isolated and closed-ended trial closes remain historical evidence; layered adequacy and open-ended guidance are not silently converted into final prompt text.
6. Confirm numeric motivator/action-attempt targets remain historical candidates and cannot override high intent, support, human request, frustration, refusal, direct action, weak engagement, or endpoints.
7. Confirm the no-specialist-transfer rule is scoped only to that receptionist variant.
8. Confirm HighLevel fields/actions remain historical mapping evidence and do not establish AutoCalls capability.
9. Confirm every action family fails closed pending authoritative capability, destination, authorization, idempotency, limit, and fallback contracts.
10. Confirm installed names/titles resolve from customer configuration and internal Top Sales Pro/template labels stay internal.
11. Confirm credential-shaped sources remain quarantined and no credential, token, private key, or secret entered the repository.
12. Run `npm test`, `npm run check`, `npm run secret-scan`, and the exact base-to-head `git diff --check`.
13. Report findings by severity with exact file and section evidence.

## Required terminal result

Return exactly one terminal line:

- `CLAUDE CHECKER: BATCH 2 NORMALIZATION ACCEPTED`
- `CLAUDE CHECKER: BATCH 2 NORMALIZATION REQUIRES CORRECTION`

Acceptance applies only to the exact reviewed commit. It authorizes no merge, Project Knowledge promotion, final prompt rewrite, deployment, external write, customer-data operation, call, message, or campaign.
