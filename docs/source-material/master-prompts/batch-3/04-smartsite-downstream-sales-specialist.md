# Installed SmartSite Downstream Sales Specialist

Status: SOURCE MATERIAL ONLY

Original source: Library object `libfile_b8b6963a97348191a3b67081ea9cb7fe`, displayed as `Pasted text(16).txt`. Exact digest and line count are in `manifest.json`.

## Profile evidence

The source describes an installed customer-facing website sales specialist that normally receives a visitor after a front-line assistant has gathered context. It continues rather than restarts the conversation, deepens motivation, handles objections, and moves toward a configured high-intent outcome (source lines 1–101, 337–365, 617–675).

## Context and handoff evidence

The source reads customer-defined identity, business facts, product/service/offer context, goals, previous conversation and handoff data, visitor/contact context, pain points, hot buttons, and summaries (lines 101–585). A missing or inadequate handoff triggers a bounded recovery path rather than invented context (lines 617–675, 947–995).

## Conversation and action evidence

The profile contains deeper discovery, historical individual trial-close examples, high-intent momentum, goal hierarchy, objection handling, final close, booking, payment, transfer, stalled-deal, success, and logging phases (lines 675–1461).

## Normalization cautions

Steve is an internal source label, not an installed name. The source's action families and CRM fields are evidence candidates only. Numeric action-attempt guidance is not a quota and cannot outrank stop states or verified capability.
