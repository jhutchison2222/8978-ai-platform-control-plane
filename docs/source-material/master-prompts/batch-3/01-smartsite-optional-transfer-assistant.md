# Installed SmartSite Sales Assistant — Optional Specialist Transfer

Status: SOURCE MATERIAL ONLY

Original source: Library object `libfile_c1afc912bdec819198c3f6a7e2ec8e07`, displayed as `Pasted text(13).txt`. Exact digest and line count are in `manifest.json`.

## Profile evidence

The source describes an installed customer-facing website Sales Assistant, not a demo agent. Its identity and business context resolve from customer custom values. It engages live website visitors, reads prior context, answers questions, qualifies, captures discoveries, and may offer a downstream specialist when deeper sales handling or a configured high-intent path calls for it (source lines 1–91, 359–413).

## Read-first context and write-back evidence

The source enumerates business identity, products, services, offers, niche, process, goals, prior conversation context, visitor/contact data, pain-point fields, hot-button fields, and summary fields (lines 91–193, 413–589). These are candidate mapping inputs, not proof that the target runtime can read or write them.

## Conversation and state evidence

Open-ended discovery, high-intent momentum, goal hierarchy, immediate action, frustration, refusal, and direct requests affect how much discovery is appropriate (lines 659–903). The source then organizes behavior into opening, need discovery, qualification, emotional deepening, business positioning, next step, appointment, payment, transfer, handoff, objection, closing, not-ready, success, and logging phases (lines 903–1565).

## Transfer evidence

The optional-transfer branch presents a downstream specialist as useful but not mandatory. Acceptance requires handoff preparation; refusal keeps the front-line assistant responsible for a suitable configured next step (lines 1265–1355).

## Normalization cautions

Internal Janet labels are not installed identity. Field names and action language do not establish AutoCalls capability. Trial-close examples and numeric action targets are historical evidence subject to the Batch 1 layered/open-ended and state-priority constraints.
