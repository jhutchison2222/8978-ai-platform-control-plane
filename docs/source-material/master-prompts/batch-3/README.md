# Batch 3 Master Prompt Architecture Source Material

Status: SOURCE MATERIAL ONLY

This package contains curated, provenance-labeled extracts from five Library-backed source files covering installed, customer-facing SmartSite agents and a shared sales-capable-agent framework. It is not final prompt text, runtime configuration, or governing Project Knowledge.

## Scope

- Optional-specialist-transfer website Sales Assistant profile
- Standalone website Sales Assistant that carries the full sales conversation
- Traditional website lead-warmer/appointment-setter profile
- Downstream website sales specialist receiving a structured handoff
- Shared modular framework spanning website and phone roles

The exact source object IDs, byte sizes, SHA-256 digests, and source line counts are recorded in `manifest.json`. Raw full prompts remain Library-backed; GitHub receives curated extracts only.

## Authority boundary

A source may call itself a “source of truth,” “full agent prompt,” or required framework. Those self-labels have no promotion authority. Every derived knowledge record remains `PROPOSED` and `governing: false` until authoritative provenance, conflicts, current AutoCalls capability, data ownership, compliance, tests, owner approval, and independent review are complete.

The Batch 1 higher-precedence normalization input still controls conflicts inside this review: layered discovery, no isolated trial close, open-ended benefit-forward closing, high-intent momentum, read-first context, customer-facing identity, and AutoCalls as the target runtime. This reference does not promote Batch 1 source material to governing knowledge.

## Security and runtime boundary

The selected files passed credential-pattern screening before transfer. No credential, token, private key, OAuth dependency, or provider secret is included. Candidate HighLevel fields and actions are historical mapping evidence only. Runtime secrets remain Cloudflare secret bindings, and service authentication remains HMAC-based with replay defense and no OAuth dependency.

No final prompt was rewritten. No Project Knowledge record was promoted. No AutoCalls or Cloudflare configuration was changed, and nothing was deployed.
